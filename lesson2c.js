// If conditional statements 
// It is used in the control flow execution of program
// It executes a block of code if a condition is true, otherwise the else block executes

let age = 20

if(age < 18) {
    console.log("Program Guide: Not are not suitable for children under the age of 18.")
}
else{
    console.log("Sit down, Relax and Enjoy...")
}
